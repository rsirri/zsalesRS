_schema-version: "3.2"
ID: zsalesrs
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: zsalesrs-destination-content
  type: com.sap.application.content
  requires:
  - name: zsalesrs-destination-service
    parameters:
      content-target: true
  - name: zsalesrs-repo-host
    parameters:
      service-key:
        name: zsalesrs-repo-host-key
  - name: zsalesrs-uaa
    parameters:
      service-key:
        name: zsalesrs-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: zsalesrs_html_repo_host
          ServiceInstanceName: zsalesrs-html5-service
          ServiceKeyName: zsalesrs-repo-host-key
          sap.cloud.service: zsalesrs
        - Authentication: OAuth2UserTokenExchange
          Name: zsalesrs_uaa
          ServiceInstanceName: zsalesrs-xsuaa-service
          ServiceKeyName: zsalesrs-uaa-key
          sap.cloud.service: zsalesrs
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: zsalesrs-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: zsalesrs-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - zsalesrs.zip
      name: zsalesrs
      target-path: resources/
- name: zsalesrs
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: zsalesrs-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: zsalesrs-destination-service
    service-plan: lite
- name: zsalesrs-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: zsalesrs-xsuaa-service
    service-plan: application
- name: zsalesrs-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: zsalesrs-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
